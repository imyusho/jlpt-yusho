export type Vocabulary = {
  expression: string;
  reading: string;
  definition: string;
  meaning: {
    en: string;
    zh: string;
  };
  example: string;
  exampleMeaning: {
    en: string;
    zh: string;
  };
};
